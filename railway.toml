[build]
dockerfilePath = "backend/Dockerfile"
buildCommand = ""

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1 --ws websockets"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
